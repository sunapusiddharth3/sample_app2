
import { printArrayValue } from 'sample-app2/helpers/print-array-value';
import { module, test } from 'qunit';

module('Unit | Helper | print array value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = printArrayValue([42]);
  assert.ok(result);
});

