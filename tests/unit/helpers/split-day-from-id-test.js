
import { splitDayFromId } from 'sample-app2/helpers/split-day-from-id';
import { module, test } from 'qunit';

module('Unit | Helper | split day from id');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = splitDayFromId([42]);
  assert.ok(result);
});

