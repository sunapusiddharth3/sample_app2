
import { decodeArrayValues } from 'sample-app2/helpers/decode-array-values';
import { module, test } from 'qunit';

module('Unit | Helper | decode array values');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = decodeArrayValues([42]);
  assert.ok(result);
});

